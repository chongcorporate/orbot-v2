// Supabase credentials for local development.
// Leave blank here — enter them via the Settings tab in the app (saved to localStorage).
// Never commit real credentials to this file.
window.ENV = {
  SUPABASE_URL: "",
  SUPABASE_SERVICE_ROLE_KEY: ""
};
